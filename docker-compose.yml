services:

  frontend:
    build:
      context: ./freto-frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./freto-backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env